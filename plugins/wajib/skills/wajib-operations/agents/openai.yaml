interface:
  display_name: "Wajib Operations"
  short_description: "Safely manage Wajib tasks and routines"
  default_prompt: "Use $wajib-operations to inspect or safely update my Wajib tasks, routines, completions, and preferences."
dependencies:
  tools:
    - type: "mcp"
      value: "wajib"
      description: "Plugin-provided local Wajib data controls"
      transport: "stdio"
policy:
  allow_implicit_invocation: true
